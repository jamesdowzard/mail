# Stalwart Mail Configuration
# Copy to .env and fill in values

# Domain
MAIL_DOMAIN=dowzard.family

# Admin credentials (generated on first run)
STALWART_ADMIN_USER=admin
STALWART_ADMIN_PASSWORD=

# SMTP Relay (if using external relay for sending)
# SMTP_RELAY_HOST=smtp.mailgun.org
# SMTP_RELAY_PORT=587
# SMTP_RELAY_USER=
# SMTP_RELAY_PASSWORD=
